`<< >>` - BUILD TIME   - Handled by gulp during deployment process
`[[ ]]` - COMPILE TIME - Handled by Sex Panther and hogan during view serving
`{{ }}` - RUN TIME     - Handled by client code (angular, backbone, polymer, etc)
